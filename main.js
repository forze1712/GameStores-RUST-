// Функция, которая будет вызвана после загрузки скрипта
function testFunction() {
    console.log("Скрипт успешно загружен и выполнен!");
}

// После загрузки скрипта, вы можете использовать его функционал
script.onload = function() {
    // Вызываем нашу тестовую функцию
    testFunction();
};
